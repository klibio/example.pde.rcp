<project xmlns="http://maven.apache.org/POM/4.0.0"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>example.pde.rcp</groupId>
  <artifactId>example.pde.rcp.parent</artifactId>
  <version>0.7.0-SNAPSHOT</version>
  <packaging>pom</packaging>

  <properties>
    <!-- plugin version -->
    <artifactory-maven-plugin-version>3.6.1</artifactory-maven-plugin-version>
    <jacoco-maven-plugin-version>0.8.12</jacoco-maven-plugin-version>
    <maven-deploy-plugin-version>3.1.2</maven-deploy-plugin-version>
    <maven-jarsigner-plugin-version>3.0.0</maven-jarsigner-plugin-version>
    <maven-release-plugin-version>3.0.1</maven-release-plugin-version>
    <maven-scm-plugin-version>2.1.0</maven-scm-plugin-version>
    <maven-gpg-plugin-version>3.2.2</maven-gpg-plugin-version>
    <build-helper-maven-plugin-version>3.6.0</build-helper-maven-plugin-version>
    <target-platform-version>0.7.0-SNAPSHOT</target-platform-version>

    <maven.build.timestamp.format>yyyyMMdd-HHmmss</maven.build.timestamp.format>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
    <tycho.scmUrl>scm:git:https://github.com:klibio/example.pde.rcp.git</tycho.scmUrl>
    <sonar-maven-plugin-version>4.0.0.4121</sonar-maven-plugin-version>
  </properties>
  <scm>
    <!-- make sure that ssh key is configured and working in target git host -->
    <connection>scm:git:https://github.com/klibio/example.pde.rcp</connection>
    <developerConnection>scm:git:https://github.com/klibio/example.pde.rcp</developerConnection>
    <url>https://github.com/klibio/example.pde.rcp</url>
  </scm>

  <distributionManagement>
    <repository>
      <id>klibio-maven-releases</id>
      <url>${env.REPOSILITE_URL}/releases/</url>
    </repository>
    <snapshotRepository>
      <uniqueVersion>true</uniqueVersion>
      <id>klibio-maven-snapshots</id>
      <url>${env.REPOSILITE_URL}/snapshots/</url>
    </snapshotRepository>
  </distributionManagement>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-deploy-plugin</artifactId>
        <version>${maven-deploy-plugin-version}</version>
      </plugin>
      <plugin>
        <groupId>org.eclipse.tycho</groupId>
        <artifactId>tycho-maven-plugin</artifactId>
        <version>${tycho-version}</version>
        <extensions>true</extensions>
      </plugin>
      <plugin>
        <groupId>org.eclipse.tycho</groupId>
        <artifactId>tycho-packaging-plugin</artifactId>
        <version>${tycho-version}</version>
        <configuration>
          <!-- FEATURE git based timestamps # PART_1_START -->
          <forceContextQualifier>${forceContextQualifier}</forceContextQualifier>
          <format>yyyyMMdd-HHmmss</format>
          <timestampProvider>jgit</timestampProvider>
          <jgit.dirtyWorkingTree>warning</jgit.dirtyWorkingTree>
          <timestampProvider>jgit</timestampProvider>
          <jgit.ignore>
            pom.xml
            .polyglot.build.properties
          </jgit.ignore>
          <sourceReferences>
            <generate>true</generate>
          </sourceReferences>
          <!-- FEATURE git based timestamps # PART_1_END -->
        </configuration>
        <!-- FEATURE git based timestamps # PART_2_START -->
        <dependencies>
          <dependency>
            <groupId>org.eclipse.tycho</groupId>
            <artifactId>tycho-buildtimestamp-jgit</artifactId>
            <version>${tycho-version}</version>
          </dependency>
          <dependency>
            <groupId>org.eclipse.tycho.extras</groupId>
            <artifactId>tycho-sourceref-jgit</artifactId>
            <version>${tycho-version}</version>
          </dependency>
        </dependencies>
        <!-- FEATURE git based timestamps # PART_2_END -->
      </plugin>
      <!-- skip the install, since we do not need the artifacts in our local mvn repo -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-install-plugin</artifactId>
        <version>2.5.2</version>
        <configuration>
          <skip>true</skip>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.eclipse.tycho</groupId>
        <artifactId>tycho-compiler-plugin</artifactId>
        <version>${tycho-version}</version>
        <configuration>
          <compilerArgs>
            <arg>-warn:-restriction</arg>
          </compilerArgs>
          <useJDK>BREE</useJDK>
          <verbose>true</verbose>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.eclipse.tycho</groupId>
        <artifactId>target-platform-configuration</artifactId>
        <version>${tycho-version}</version>
        <configuration>
          <target>
            <artifact>
              <groupId>${project.groupId}</groupId>
              <artifactId>target-platform</artifactId>
              <version>${target-platform-version}</version>
            </artifact>
          </target>
          <environments>
            <environment>
              <os>macosx</os>
              <ws>cocoa</ws>
              <arch>aarch64</arch>
            </environment>
            <environment>
              <os>macosx</os>
              <ws>cocoa</ws>
              <arch>x86_64</arch>
            </environment>
            <environment>
              <os>linux</os>
              <ws>gtk</ws>
              <arch>aarch64</arch>
            </environment>
            <environment>
              <os>linux</os>
              <ws>gtk</ws>
              <arch>x86_64</arch>
            </environment>
            <environment>
              <os>win32</os>
              <ws>win32</ws>
              <arch>x86_64</arch>
            </environment>
          </environments>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.eclipse.tycho</groupId>
        <artifactId>tycho-source-plugin</artifactId>
        <version>${tycho-version}</version>
        <executions>
          <execution>
            <id>plugin-source</id>
            <goals>
              <goal>generate-pde-source-header</goal>
              <goal>feature-source</goal>
              <goal>plugin-source</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.eclipse.tycho</groupId>
        <artifactId>tycho-p2-plugin</artifactId>
        <version>${tycho-version}</version>
        <executions>
          <execution>
            <id>attach-p2-metadata</id>
            <phase>package</phase>
            <goals>
              <goal>p2-metadata</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.eclipse.tycho</groupId>
        <artifactId>tycho-p2-repository-plugin</artifactId>
        <version>${tycho-version}</version>
        <configuration>
          <includeAllDependencies>true</includeAllDependencies>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.eclipse.tycho</groupId>
        <artifactId>tycho-versions-plugin</artifactId>
        <version>${tycho-version}</version>
      </plugin>
      <plugin>
        <groupId>com.github.spotbugs</groupId>
        <artifactId>spotbugs-maven-plugin</artifactId>
        <version>3.1.3</version>
        <configuration>
          <effort>Max</effort>
          <threshold>Low</threshold>
          <failOnError>false</failOnError>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.eclipse.tycho</groupId>
        <artifactId>tycho-p2-director-plugin</artifactId>
        <version>${tycho-version}</version>
        <configuration>
          <products>
            <product>
              <id>example.rcp.app.ui.feature.product</id>
              <archiveFileName>example.rcp.app.ui.feature.product-${project.version}.${buildQualifier}</archiveFileName>
              <rootFolder>example-rcp-app</rootFolder>
            </product>
            <product>
              <id>example.rcp.app.ui.plugin.product</id>
              <archiveFileName>example.rcp.app.ui.plugin.product-${project.version}.${buildQualifier}</archiveFileName>
              <rootFolder>example-rcp-app</rootFolder>
            </product>
            <product>
              <id>example.rcp.app.ui.mixed.product</id>
              <archiveFileName>example.rcp.app.ui.mixed.product-${project.version}.${buildQualifier}</archiveFileName>
              <rootFolder>example-rcp-app</rootFolder>
            </product>
            <product>
              <id>example.rcp.headless.feature.product</id>
              <archiveFileName>example.rcp.headless.feature.product-${project.version}.${buildQualifier}</archiveFileName>
              <rootFolder>example-rcp-headless</rootFolder>
            </product>
          </products>
          <formats>
            <linux>zip</linux>
            <win32>zip</win32>
          </formats>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.eclipse.tycho</groupId>
        <artifactId>tycho-surefire-plugin</artifactId>
        <version>${tycho-version}</version>
        <configuration>
          <includes>
            <include>**/*Test.java</include>
            <include>**/*Tests.java</include>
          </includes>
          <useUIHarness>false</useUIHarness>
          <useUIThread>false</useUIThread>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.jacoco</groupId>
        <artifactId>jacoco-maven-plugin</artifactId>
        <version>${jacoco-maven-plugin-version}</version>
        <executions>
          <execution>
            <goals>
              <goal>prepare-agent</goal>
            </goals>
            <configuration>
              <excludes>
                <exclude>java/**</exclude>
                <exclude>javax/**</exclude>
                <exclude>jdk/**</exclude>
                <exclude>sun/**</exclude>
                <exclude>com/sun/**</exclude>
                <exclude>org/jcp/**</exclude>
              </excludes>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.sonarsource.scanner.maven</groupId>
        <artifactId>sonar-maven-plugin</artifactId>
        <version>${sonar-maven-plugin-version}</version>
        <configuration>
          <properties>
            <property>
              <name>sonar.jacoco.reportPaths</name>
              <value>${basedir}/tests/example.rcp.tests.it/target/jacoco.exec</value>
            </property>
            <property>
              <name>sonar.coverage.jacoco.xmlReportPaths</name>
              <value>
                ${basedir}/tests/example.rcp.tests/reports/target/site/jacoco-aggregate/jacoco.xml</value>
            </property>
            <property>
              <name>sonar.exclusions</name>
              <value>.polyglot*, .tycho*,</value>
            </property>
          </properties>
        </configuration>
      </plugin>
    </plugins>
  </build>

  <modules>
    <module>bundles</module>
    <module>features</module>
    <module>comp1/bundles</module>
    <module>comp1/features</module>
    <module>comp2/bundles</module>
    <module>comp2/features</module>
    <module>releng</module>
    <module>tests</module>
  </modules>

  <profiles>
    <profile>
      <id>deploy-maven-to-artifactory</id>
      <activation>
        <property>
          <name>deploy-maven-to-artifactory</name>
          <value>true</value>
        </property>
      </activation>
      <build>
        <plugins>
          <plugin>
            <groupId>org.jfrog.buildinfo</groupId>
            <artifactId>artifactory-maven-plugin</artifactId>
            <version>${artifactory-maven-plugin-version}</version>
            <executions>
              <execution>
                <id>upload-bundles</id>
                <goals>
                  <goal>publish</goal>
                </goals>
                <configuration>
                  <deployProperties>
                    <someProperty1>awesome</someProperty1>
                  </deployProperties>
                  <artifactory>
                    <includeEnvVars>true</includeEnvVars>
                    <envVarsExcludePatterns>*password*,*secret*,*key*,*token*,*passphrase*</envVarsExcludePatterns>
                    <timeoutSec>60</timeoutSec>
                  </artifactory>
                  <publisher>
                    <contextUrl>${contextUrl}</contextUrl>
                    <username>${username}</username>
                    <password>${token}</password>
                    <repoKey>klibio-main-maven-releases</repoKey>
                    <snapshotRepoKey>klibio-main-maven-snapshots</snapshotRepoKey>
                    <publishArtifacts>true</publishArtifacts>
                    <publishBuildInfo>true</publishBuildInfo>
                  </publisher>
                  <buildInfo>
                    <buildName>${project.artifactId}</buildName>
                    <buildNumber>${buildnumber}</buildNumber>
                    <buildUrl>http://build-url.org</buildUrl>
                  </buildInfo>
                </configuration>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
    <profile>
      <id>deploy-p2</id>
      <activation>
        <property>
          <name>deploy-p2-repos</name>
          <value>true</value>
        </property>
      </activation>
      <build>
        <plugins>
          <plugin>
            <groupId>org.jfrog.buildinfo</groupId>
            <artifactId>artifactory-maven-plugin</artifactId>
            <version>${artifactory-maven-plugin-version}</version>
            <executions>
              <execution>
                <id>upload-p2-repositories</id>
                <goals>
                  <goal>publish</goal>
                </goals>
                <configuration>
                  <deployProperties>
                    <someProperty1>xyz</someProperty1>
                  </deployProperties>
                  <artifactory>
                    <includeEnvVars>true</includeEnvVars>
                    <envVarsExcludePatterns>*password*,*secret*,*key*,*token*,*passphrase*</envVarsExcludePatterns>
                    <timeoutSec>60</timeoutSec>
                  </artifactory>
                  <publisher>
                    <contextUrl>${contextUrl}</contextUrl>
                    <username>${username}</username>
                    <password>${token}</password>
                    <repoKey>klibio-internet-mirror-generic-local</repoKey>
                    <snapshotRepoKey>klibio-internet-mirror-generic-local</snapshotRepoKey>
                    <publishArtifacts>true</publishArtifacts>
                    <publishBuildInfo>true</publishBuildInfo>
                    <includePatterns>**/*</includePatterns>
                  </publisher>
                  <buildInfo>
                    <buildName>example.pde.rcp</buildName>
                    <buildNumber>${buildnumber}</buildNumber>
                  </buildInfo>
                </configuration>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>

    <profile>
      <id>perform-jar-signing</id>
      <activation>
        <property>
          <name>perform-jar-signing</name>
          <value>true</value>
        </property>
      </activation>
      <build>
        <plugins>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-jarsigner-plugin</artifactId>
            <version>${maven-jarsigner-plugin-version}</version>
            <executions>
              <execution>
                <id>sign</id>
                <goals>
                  <goal>sign</goal>
                </goals>
              </execution>
            </executions>
            <configuration>
              <alias>${sign_alias}</alias>
              <keypass>${sign_keypass}</keypass>
              <keystore>${keystore}</keystore>
              <skip>${sign_skip}</skip>
              <storepass>${sign_storepass}</storepass>
              <verbose>${sign_verbose}</verbose>
            </configuration>
          </plugin>
        </plugins>
      </build>
    </profile>
    <profile>
      <id>perform-gpg-signing</id>
      <activation>
        <property>
          <name>perform-gpg-signing</name>
          <value>true</value>
        </property>
      </activation>
      <build>
        <plugins>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-gpg-plugin</artifactId>
            <version>${maven-gpg-plugin-version}</version>
            <executions>
              <execution>
                <id>sign-artifacts</id>
                <phase>verify</phase>
                <goals>
                  <goal>sign</goal>
                </goals>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>